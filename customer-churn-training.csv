import pandas as pd
from sklearn.metrics import classification_report, confusion_matrix
from sklearn.linear_model import LogisticRegression
from sklearn.model_selection import train_test_split

# Load dataset
df = pd.read_csv('customer-churn-training.csv')

# Define features and target
X = pd.get_dummies(df[['tenure_months', 'support_tickets', 'monthly_spend_inr', 'last_login_days', 'plan_type']], drop_first=True)
y = df['churned']

# Rule-based baseline evaluation function
def rule_based_baseline(row):
    # Rule: support_tickets > 2 or last_login_days > 14
    if row['support_tickets'] > 2 or row['last_login_days'] > 14:
        return 1
    return 0

df['baseline_pred'] = df.apply(rule_based_baseline, axis=1)
print("--- Rule-Based Baseline Confusion Matrix ---")
print(confusion_matrix(df['churned'], df['baseline_pred']))
